# Пример конфигурации для Docker.
# Скопируйте в config/telemt-admin.toml и укажите bot_token, admin_ids, при необходимости auth_header для control API.

bot_token = "УКАЖИТЕ_ТОКЕН_ОТ_BOTFATHER"
admin_ids = [123456789]

# Режим external и метка для UI задаются в docker-compose.yml (TELEMT_ADMIN__RUNTIME__*).

db_path = "/var/lib/telemt-admin/state.db"
telemt_config_path = "/etc/telemt.toml"

[telemt_api]
enabled = true
base_url = "http://telemt:9091"
# auth_header = "Bearer ..."  # при необходимости; иначе можно через TELEMT_ADMIN__TELEMT_API__AUTH_HEADER в .env
timeout_ms = 5000
allow_file_fallback = false
prefer_api_links = true

[notifications]
enabled = true
health_check_interval_secs = 60
notify_on_health_change = true
notify_on_runtime_alerts = true
notify_on_new_request = true

[security]
default_token_days = 14
max_token_days = 180
allow_auto_approve_tokens = true

[bot_messages]
# При /start без invite-токена можно отдать нейтральный текст-заглушку и не переводить
# пользователя сразу в wizard ввода токена.
# start_without_invite = "Привет. Бот сейчас работает в закрытом режиме."
#
# Простые шаблоны без отдельного движка; доступны плейсхолдеры вроде {link}, {action}, {id} и т.д.
# user_link_template = "Ваша ссылка:\n\n{link}"
# access_approved_template = "Доступ готов.\n\n{link}"
# request_submitted = "Заявка отправлена. Мы свяжемся с вами."
# request_pending = "Заявка уже на рассмотрении."
# request_rejected = "Заявка отклонена."
# broadcast_prompt = "Рассылка {audience}. Отправьте текст следующим сообщением."
# broadcast_summary_template = "Рассылка завершена. Успешно: {ok}, ошибок: {failed}, всего: {total}."
# no_access_status_text = "Чтобы получить доступ, отправьте /start и введите invite-токен."
# no_access_link_text = "У вас нет доступа к прокси. Отправьте /start для регистрации."
#
# admin_home_text = "Панель администратора\n\nВыберите раздел ниже."
# user_home_approved = "Доступ уже открыт.\n\nНажмите «Получить ссылку»."
# user_home_pending = "Заявка уже на рассмотрении.\n\nДождитесь решения администратора."
# user_home_rejected = "Заявка отклонена.\n\nЕсли есть новый invite-токен, отправьте /start и введите его заново."
# user_home_deleted = "Доступ был отозван.\n\nДля новой регистрации отправьте /start и введите invite-токен заново."
# usage_guide_text = "Как подключиться к прокси: получите ссылку через /link и нажмите на неё."
# help_admin_text = "Команды: /start, /user, /token, /service, /link, /help."
# help_user_text = "Команды: /start, /help."
# unknown_command_text = "Неизвестная команда. Используйте /help."
# admin_only_deep_link_text = "Этот deep link доступен только администраторам."
# user_not_found_text = "Пользователь не найден или уже неактивен."
# token_not_found_text = "Токен не найден или уже недоступен."
# service_action_confirm_template = "Подтвердить действие: {action}?"
# token_error_not_found = "Токен не найден. Проверьте код и попробуйте снова."
# token_error_revoked = "Этот токен отозван администратором."
# token_error_expired = "Срок действия invite-токена истёк."
# token_error_usage_limit = "Лимит активаций invite-токена исчерпан."
# admin_notify_new_request_template = "📋 Новая заявка #{id}: User ID: {user_id}, Username: @{username}, Имя: {display_name}, Время: {time}"
# admin_notify_auto_approve_template = "✅ Автоподключение по invite-токену: User ID: {user_id}, Username: @{username}, Token: {token}"
# fallback_unknown_request_text = "Не понял запрос. Используйте /help или начните сценарий через кнопку."
